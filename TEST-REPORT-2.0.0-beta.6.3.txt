觅阅 2.0.0-beta.6.3 测试报告

已完成：
1. 全部 43 个 Lua 文件语法检查通过。
2. 模拟 error_code=-10102 连续两次后恢复，确认自动等待并成功继续。
3. 模拟章节原始内容接口返回频率限制，确认同样能够等待后继续。
4. 模拟连续四次频率限制，确认只请求四次，等待 15/30/60 秒后停止。
5. 自动等待全部失败后，确认错误包含可识别标记并保留断点。
6. 模拟等待期间取消下载，确认立即停止且不会发出额外请求。
7. 下载进度新增“等待请求恢复”，保留原下载百分比并显示剩余秒数。
8. 普通前台请求默认不启用长时间等待，避免书架界面长时间阻塞。
9. _meta.lua 与 config.lua 版本一致。
10. 全量安装包目录、文件完整性、版本号、大小和 SHA-256 校验通过。
11. 插件目录不包含 .md、日志、EPUB、设置文件或临时文件。

仍需实机确认：
1. 微信读书真实 -10102 限制的解除时间可能因账号和网络环境不同。
2. 超过 105 秒仍未解除限制时，需要稍后重新点击下载。
3. Android 长书下载时 0.45 秒请求间隔对总耗时的实际影响。
