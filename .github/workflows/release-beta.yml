name: 发布内测版

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: miuread-release-beta
  cancel-in-progress: false

jobs:
  release:
    name: 构建并发布内测包
    runs-on: ubuntu-latest
    steps:
      - name: 读取源码
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 检查分支与版本
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "beta" || {
            echo "此工作流只能在 beta 分支运行，当前分支为 $GITHUB_REF_NAME"
            exit 1
          }
          test "$(cat CHANNEL)" = "beta" || {
            echo "CHANNEL 与工作流不一致"
            exit 1
          }
          python3 scripts/build_release.py --channel beta --check-only

      - name: 生成发布文件
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/build_release.py --channel beta

      - name: 创建或更新 GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          VERSION="$(cat VERSION)"
          TAG="v${VERSION}"
          TITLE="觅阅 ${VERSION}（内测）"
          ASSETS=(dist/*.zip dist/*.json dist/SHA256SUMS.txt dist/RELEASE_NOTES-*.md)
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${ASSETS[@]}" --clobber
            gh release edit "$TAG" --title "$TITLE" --notes-file RELEASE_NOTES.md --prerelease
          else
            gh release create "$TAG" "${ASSETS[@]}" --target "$GITHUB_SHA" --title "$TITLE" --notes-file RELEASE_NOTES.md --prerelease
          fi

      - name: 写回更新清单
        shell: bash
        run: |
          set -euo pipefail
          cp "dist/update-beta.json" "update-beta.json"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "update-beta.json"
          if ! git diff --cached --quiet; then
            git commit -m "chore: update beta manifest for v$(cat VERSION)"
            git push origin "HEAD:beta"
          fi
